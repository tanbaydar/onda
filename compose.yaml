services:
  db:
    image: mysql:8.4
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${ONDA_DB_NAME}
      MYSQL_USER: ${ONDA_DB_USER}
      MYSQL_PASSWORD: ${ONDA_DB_PASSWORD}
      MYSQL_ROOT_PASSWORD: ${ONDA_DB_ROOT_PASSWORD}
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -u$$MYSQL_USER -p$$MYSQL_PASSWORD --silent"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  web:
    build:
      context: .
      dockerfile: infrastructure/docker/backend.Dockerfile
    restart: unless-stopped
    env_file: .env
    environment:
      DJANGO_SETTINGS_MODULE: config.settings_production
      ONDA_DB_HOST: db
      ONDA_DB_PORT: "3306"
      ONDA_MEDIA_ROOT: /app/media
    command: >-
      gunicorn config.wsgi:application --bind 0.0.0.0:8000
      --workers 2 --access-logfile - --error-logfile -
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - django_static:/app/staticfiles
      - media_data:/app/media
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; request = urllib.request.Request('http://127.0.0.1:8000/api/cities/', headers={'X-Forwarded-Proto': 'https'}); urllib.request.urlopen(request, timeout=5)"]
      interval: 15s
      timeout: 6s
      retries: 5
      start_period: 30s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  caddy:
    build:
      context: .
      dockerfile: infrastructure/docker/frontend.Dockerfile
    restart: unless-stopped
    environment:
      ONDA_DOMAIN: ${ONDA_DOMAIN}
      CADDY_ACME_EMAIL: ${CADDY_ACME_EMAIL}
    depends_on:
      web:
        condition: service_healthy
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - django_static:/srv/django-static:ro
      - media_data:/srv/media:ro
      - caddy_data:/data
      - caddy_config:/config
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  mysql_data:
  django_static:
  media_data:
  caddy_data:
  caddy_config:
