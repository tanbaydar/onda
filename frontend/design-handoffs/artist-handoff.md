# ARTIST PAGE — identity and event-ledger ruling

Ruled 2026-08-27 by the current operator. Scope: Artist detail at mobile and desktop widths. This file consolidates the earlier portrait and favorite deltas into a complete surface authority.

## Identity

- Artist identity uses the shared circular portrait grammar: center-cropped portrait when present; neutral head-and-shoulders silhouette when absent or failed.
- The artist name is the only primary identity text and uses the display face at the detail title scale. Do not add an eyebrow, category label, invented biography, external links, or promotional description without a later ruling.
- A signed-in viewer's word + heart favorite control sits with the identity copy. Guests see no disabled or solicitation state.
- Identity remains flat on white with no card, background panel, shadow, or decorative image.

## Event collections

- Upcoming then Past use the shared compact Event ledger and existing 4:5 event-flier grammar.
- Each section uses the standard heading, loading, local error/Retry, empty line, and pagination anatomy. Empty copy is `No upcoming events.` / `No past events.`
- The current artist is omitted from compact lineup repetition when the shared event-row presenter supports that omission.
- One-page collections have no pagination chrome. Continuation failure preserves successful rows.

## Responsive composition

- Mobile uses the shared compact catalog-identity composition; desktop uses the shared 160px identity portrait and one continuous content column.
- Long names wrap without shrinking the display role or colliding with the favorite control.
- No Artist-specific color is introduced. Judgment green appears only in committed favorite state or judgment data already ruled for shared event rows.

## States

- Initial loading uses the system ellipsis convention. Initial failure offers local Retry. Not found uses a stable title, explanation, and Return to Discover action.
- Failed portrait loading falls back to the same silhouette without layout shift.
